#!/usr/bin/python

data = {"timestamp": None,
        "releasestr": None,
        "arch": None,
        "discNum": None,
        "baseDir": None,
        "packagesDir": None,
        "pixmapsDir": None,
        "outfile": None}

for index in range(1000):
	print index;

for index in range(len(data)):
	print data[index];  

