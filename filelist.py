
def chomp(line):
    if line[-1] == '\n':
        line = line[:-1]
    return line;

file_list=map( chomp, open(file).readlines()):

def get_file_list_from_file(file)
for each in 
    #print "%s->%s"%(chomp(each),  len(open(each).readlines()) );

    #print "%s"%(chomp(each));
    #print chomp(each);
